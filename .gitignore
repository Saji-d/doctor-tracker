# Local assignment materials only — not part of the submitted deliverable.
# Root README.md IS tracked (GitHub renders it as the repo landing page,
# which is exactly where a monorepo's overview belongs).
# backend/ and frontend/ each have their own .gitignore for their own build
# artifacts, dependencies, and secrets.
/*.pdf
/Screenshot*.png
/IMPLEMENTATION_PLAN.md
/.claude/
